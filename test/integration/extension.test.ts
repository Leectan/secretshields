/**
 * Integration smoke test placeholder.
 *
 * Full integration tests require @vscode/test-electron to launch
 * a real VS Code instance. This file documents the test plan for
 * manual and CI integration testing.
 *
 * To run integration tests:
 *   npm run test:integration
 *
 * Test plan:
 * 1. Extension activates without errors
 * 2. "SecretShields: Mask Clipboard Now" command is registered
 * 3. "SecretShields: Restore Last Secret" command is registered
 * 4. Status bar item appears
 * 5. Exposure Log tree view is registered
 * 6. Copying a test secret masks the clipboard (manual)
 * 7. Restoring creates an exposure event (manual)
 */

// This file is intentionally a placeholder.
// Integration tests are run via @vscode/test-electron, not vitest.
